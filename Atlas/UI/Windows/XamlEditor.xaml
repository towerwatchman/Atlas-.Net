<Window x:Class="Atlas.UI.Windows.XamlEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
        Title="XAML Editor"
        Height="600"
        Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0"
                 Grid.ColumnSpan="2">
            <Button Click="NewButton_Click"
                    Content="New" />
            <Button Click="LoadButton_Click"
                    Content="Load" />
            <Button Click="SaveButton_Click"
                    Content="Save" />
        </ToolBar>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <avalonEdit:TextEditor x:Name="tbXamlEditor"
                                   Grid.Column="0"
                                   Margin="5"
                                   ShowLineNumbers="True"
                                   SyntaxHighlighting="XAML"
                                   VerticalScrollBarVisibility="Auto"
                                   HorizontalScrollBarVisibility="Auto"
                                   TextChanged="XamlEditor_TextChanged" />
            <Border x:Name="PreviewBorder"
                    Grid.Column="1"
                    Margin="5"
                    Background="White"
                    BorderBrush="Gray"
                    BorderThickness="1">
                <ContentControl x:Name="PreviewControl" />
            </Border>
        </Grid>
    </Grid>
</Window>