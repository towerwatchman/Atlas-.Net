<Page x:Class="Atlas.UI.Pages.BannerViewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
      xmlns:my="clr-namespace:Atlas.UI.Pages"
      xmlns:custom="clr-namespace:Atlas.UI"
      xmlns:vm="clr-namespace:Atlas.UI.ViewModel"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="BannerViewPage">
    <Grid>
        <ListView x:Name="BannerView"
                  Margin="0,2,0,0"
                  Background="{DynamicResource Primary}"
                  Foreground="{DynamicResource Text}"
                  BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  HorizontalAlignment="Stretch"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.IsDeferredScrollingEnabled="False"
                  MaxHeight="10000">
            <ListView.Template>
                <ControlTemplate>
                    <ScrollViewer Background="{DynamicResource Primary}">
                        <vwp:VirtualizingWrapPanel Background="{DynamicResource Panel}"
                                                   
                                                   IsItemsHost="True" />
                    </ScrollViewer>
                </ControlTemplate>
            </ListView.Template>
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:GameViewModel}">
                    <Grid Margin="4">
                        <!--<Image Source="{Binding BannerImage, IsAsync=True}"
                               Stretch="Uniform"
                               Height="251"
                               Width="537" />-->
                        <custom:GameBanner x:Name="GameBannerItem" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch" />
                    <Setter Property="BorderThickness"
                            Value="0" />
                    <Setter Property="Padding"
                            Value="0" />
                    <Setter Property="Margin"
                            Value="0" />
                    <Setter Property="RenderTransformOrigin"
                            Value="0.5,0.5" />
                    <!--<Style.Triggers>
                        <Trigger Property="IsMouseOver"
                                 Value="True">
                            <Setter Property="Panel.ZIndex"
                                    Value="1000" />
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.01"
                                                    ScaleY="1.01" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>-->
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.Resources></ListView.Resources>
            <ListView.ContextMenu>
                <ContextMenu x:Name="cmGame"
                             DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"></ContextMenu>
            </ListView.ContextMenu>
        </ListView>
    </Grid>
</Page>
